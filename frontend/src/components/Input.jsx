import React from 'react';

export default function Input(props) {
  return (
    <input {...props}>
      {/* 
        TODO:
        - Design custom labels, error messages, and descriptions for validation inputs
      */}
    </input>
  );
}
